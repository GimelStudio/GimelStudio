// import 'dart:io';
import 'package:flutter/foundation.dart' show kIsWeb;

const bool isWeb = kIsWeb;
// final bool isLinux = !isWeb && Platform.isLinux;
// final bool isMacOS = !isWeb && Platform.isMacOS;
// final bool isWindows = !isWeb && Platform.isWindows;
// final bool isDesktop = isLinux || isMacOS || isWindows;
